#pragma once
#include <vector>
#include <string>

class leitor {
    public:
        std::vector<std::vector<int>> matriz;
        std::string identificador; // guarda o nome da instância

        leitor();
};
